{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null",
        ".write": "$uid === auth.uid"
      }
    },
    "priceCache": {
      "$coin": {
        ".read": true,
        ".write": "auth != null"
      }
    }
  }
}
