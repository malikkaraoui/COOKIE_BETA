# Variables d'environnement Firebase pour le projet COOKIE
# Template pour créer votre fichier .env local
# Obtenir ces valeurs depuis Firebase Console > Project Settings > General

# Clé API publique - utilisée pour identifier le projet Firebase côté client
VITE_FIREBASE_API_KEY=your_api_key_here

# Domaine d'authentification - URL autorisée pour les redirections OAuth (Google Sign-In)
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com

# ID unique du projet Firebase - identifie votre projet dans l'écosystème Firebase
VITE_FIREBASE_PROJECT_ID=your-project-id

# Bucket de stockage - pour Firebase Storage (stockage de fichiers/images)
VITE_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app

# ID de l'expéditeur pour Firebase Cloud Messaging (notifications push)
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012

# ID unique de l'application web - identifie cette application spécifique dans le projet
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# Note: Le prefixe VITE_ est obligatoire pour que Vite expose ces variables au code client
# Ces variables sont accessibles via import.meta.env.VITE_FIREBASE_API_KEY etc.

# IMPORTANT : Ne jamais commit le fichier .env dans Git
# Le fichier .env est déjà dans .gitignore
